initSidebarItems({"macro":[["loop_m128","Macro `loop_m128` is the main loop that searches in byte slice with a bit mask using `sse4.2` optimizations."],["loop_m256_128","Macro `loop_m256_128` is the main loop that searches in byte slice with a bit mask using `avx2` optimizations."],["new_escape","Generates struct `$name` with escaping functionality at `fmt`"],["new_escape_sized","Generates struct `$name` with escaping functionality at `fmt` and `size` method"]]});